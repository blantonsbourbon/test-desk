# Domain documentation

This repository uses a single domain context:

- `CONTEXT.md` is the canonical glossary and ubiquitous language.
- `docs/adr/` contains accepted architectural decisions.
- Specifications belong under `docs/specs/`.

Consumers should use the glossary terms in `CONTEXT.md` and treat current-state
architecture documents as implementation evidence unless an ADR or target
specification supersedes them.
