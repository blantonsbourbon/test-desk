spring:
  application:
    name: testdesk

server:
  port: 8080
  servlet:
    context-path: /

testdesk:
  cors:
    allowed-origins: "http://localhost:3000,http://localhost:5173,http://localhost:4200"
