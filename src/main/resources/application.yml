spring:
  application:
    name: test-desk

server:
  port: 8080
  servlet:
    context-path: /

test-desk:
  cors:
    allowed-origins: "http://localhost:3000,http://localhost:5173,http://localhost:4200"
